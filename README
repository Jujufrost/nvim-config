This is my personalized configuration of NeoVim.
This uses the lazy nvim package manager.

--- Adding plugin ---
Navigate to plugins directory.
Create a new lua file with plugin name.
Return a table with the plugin link at the top.
Configure plugin.
